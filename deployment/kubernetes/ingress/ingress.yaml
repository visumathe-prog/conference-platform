apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: conference-ingress
  namespace: conference
spec:
  rules:
  - host: conference.local
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: api-gateway
            port:
              number: 80
