{
  "ConnectionStrings": {
    "PostgreSQL": "Host=postgres-identity;Port=5432;Database=identity_db;Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}"
  },
  "Redis": {
    "ConnectionString": "redis:6379,password=${REDIS_PASSWORD}"
  },
  "Kafka": {
    "BootstrapServers": "kafka:9092"
  }
}
